<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="nav-link" aria-current="page" href="{{ path('home') }}"><img class="d-block w-25" src="https://placeholder.com/wp-content/uploads/2018/10/placeholder.com-logo3.jpg" alt=""></a>
		<a class="navbar-brand" href="#">Let's Go !</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">			
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('home') }}">Accueil<span class="sr-only"></span></a>
				</li>
        			<a class="nav-link" aria-current="page" href="{{ path('quizz_list') }}">Liste des quizz<span class="sr-only"></span></a>
				</li>
			{% if not is_granted('ROLE_USER') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_register') }}">inscription<span class="sr-only"></span></a>
				</li>		
			{% endif %}
			</ul>
			<div class="d-flex">
				{% if is_granted('ROLE_USER') %}
				<div class="text-center mt-2">
					<p>{{ app.user.login }}</p>
				</div>
				<div class="dropdown">
					<button class="btn btn-secondary dropdown-toggle ms-3 me-3" type="button" id="dropdownMenu2" data-bs-toggle="dropdown" aria-expanded="false">
						    MENU
					</button>
					<ul class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenu2">
						<li><a class="dropdown-item" href="{{ path('user_profil', {'id': app.user.id}) }}" type="button">Profil</a></li>
						<li><hr class="dropdown-divider"></li>
						<li><a class="dropdown-item" href="{{ path('app_logout') }}" type="button">Déconnexion</a></li>
					</ul>
				</div>
				{% else %}
					<a class="btn btn-lg btn-primary" href="{{ path('app_login') }}" role="button">Connexion</a>
					{# option class="btn btn-primary"#}
					{# <button class="btn btn-outline-success" type="submit">Search</button> #}
				{% endif %}
			</div>
		</div>
	</div>
</nav>