<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="nav-link" aria-current="page" href="{{ path('home') }}"><img class="d-block w-25" src="https://placeholder.com/wp-content/uploads/2018/10/placeholder.com-logo3.jpg" alt=""></a>
		<a class="navbar-brand" href="#">Let's Go !</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">			
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('home') }}">Home<span class="sr-only"></span></a>
				</li>
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('contact') }}">Contact<span class="sr-only"></span></a>
				</li>
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('team') }}">Qui sommes nous ?<span class="sr-only"></span></a>
				</li>
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('notice') }}">Mentions légales<span class="sr-only"></span></a>
				</li>
        <a class="nav-link" aria-current="page" href="{{ path('quizz_list') }}">Liste des quizz<span class="sr-only"></span></a>
				</li>
			{% if not is_granted('ROLE_USER') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_register') }}">inscription<span class="sr-only"></span></a>
				</li>		
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_login') }}">Login<span class="sr-only"></span></a>
				</li>
			{% endif %}
			</ul>
			<div class="d-flex">
				{% if is_granted('ROLE_USER') %}
					
					<a class="btn btn-lg btn-dark" href="{{ path('app_logout') }}" role="button">Déconnexion</a>
				{% else %}
					<a class="btn btn-lg btn-primary" href="{{ path('app_login') }}" role="button">Connexion</a>
					{# option class="btn btn-primary"#}
					{# <button class="btn btn-outline-success" type="submit">Search</button> #}
				{% endif %}
			</div>
		</div>
	</div>
</nav>